import { JsonUtil } from '../../util/json-util.js';

function feedFromChat(chat) {
    let feed = { feedType: -999999 };
    try {
        feed = { ...feed };
        if (chat.text) {
            Object.assign(feed, JsonUtil.parseLoseless(chat.text));
        }
        return feed;
    }
    catch (e) {
        return feed;
    }
}
function feedToText(feed) {
    return JsonUtil.stringifyLoseless(feed);
}

export { feedFromChat, feedToText };
//# sourceMappingURL=chat-feed.js.map
