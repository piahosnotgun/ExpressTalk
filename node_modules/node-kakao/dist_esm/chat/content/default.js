class TextContent {
    constructor(text) {
        this.text = text;
    }
    append(chat) {
        chat.text += this.text;
    }
}
class AttachmentContent {
    constructor(attachment) {
        this.attachment = attachment;
    }
    append(chat) {
        if (!chat.attachment)
            return;
        chat.attachment = Object.assign(chat.attachment, this.attachment);
    }
}

export { AttachmentContent, TextContent };
//# sourceMappingURL=default.js.map
