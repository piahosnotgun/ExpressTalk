import { isNode } from '../util/platform.js';

async function newCryptoStore(pubKey) {
    if (isNode()) {
        return (await import('./node-crypto-store.js')).createNodeCrypto(pubKey);
    }
    else {
        return (await import('./forge-crypto-store.js')).createForgeCrypto(pubKey);
    }
}

export { newCryptoStore };
//# sourceMappingURL=index.js.map
