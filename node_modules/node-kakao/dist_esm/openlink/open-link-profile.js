import { OpenProfileType } from './open-link-type.js';

var OpenLinkProfile;
(function (OpenLinkProfile) {
    function serializeLinkProfile(template) {
        if ('linkId' in template) {
            return { ptp: OpenProfileType.OPEN_PROFILE, pli: template.linkId };
        }
        else if ('nickname' in template) {
            return { ptp: OpenProfileType.KAKAO_ANON, nn: template.nickname, pp: template.profilePath };
        }
        else {
            return { ptp: OpenProfileType.MAIN };
        }
    }
    OpenLinkProfile.serializeLinkProfile = serializeLinkProfile;
})(OpenLinkProfile || (OpenLinkProfile = {}));

export { OpenLinkProfile };
//# sourceMappingURL=open-link-profile.js.map
