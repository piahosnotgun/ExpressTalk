import { deserialize as bson_53, serialize as bson_55 } from '../node_modules/bson/dist/bson.browser.esm.js';
import { Buffer } from 'buffer';

const BsonDataCodec = {
    canDecode(dataType) {
        return dataType == 0 || dataType == 8;
    },
    decode(data) {
        return bson_53(Buffer.from(data), {
            promoteLongs: false,
        });
    },
    encode(data) {
        const buf = bson_55(data);
        return [0, buf];
    },
};

export { BsonDataCodec };
//# sourceMappingURL=bson-data-codec.js.map
